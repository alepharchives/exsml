# Makefile for dynamic library interface to execv etc

# Makefile for Moscow ML dynamic libraries: the crypt example

AM_CFLAGS = -I$(top_srcdir)/runtime -I$(top_srcdir) -fno-defer-pop

lib_LTLIBRARIES = libsml_unix.la
libsml_unix_la_SOURCES = unix.c



