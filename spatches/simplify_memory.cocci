@@
expression FP, VAL;
@@

-Modify(FP, VAL)
+modify(fp, val)

@@
expression NAME, SIZE;
@@

-Push_roots(NAME, SIZE)
+PUSH_ROOTS(NAME, SIZE)

@@
@@

-Pop_roots()
+POP_ROOTS()

@@
expression RESULT, WOSIZE, TAG;
@@

-Alloc_small(RESULT, WOSIZE, TAG)
+ALLOC_SMALL(RESULT, WOSIZE, TAG)
